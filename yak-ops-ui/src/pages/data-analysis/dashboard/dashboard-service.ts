/**
 * @deprecated New dashboard code should import from `@/services/dashboard`.
 * Existing editor and viewer imports remain supported during incremental migration.
 */
export * from '@/services/dashboard';
