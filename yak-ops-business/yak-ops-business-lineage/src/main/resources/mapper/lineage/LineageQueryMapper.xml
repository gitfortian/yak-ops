<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.yak.ops.business.lineage.dao.mapper.LineageQueryMapper">

    <select id="selectAssetIdsByEvidence" resultType="java.lang.Long">
        SELECT source_asset_id AS asset_id
        FROM yak_metadata_relation
        WHERE source_type = #{sourceType} AND source_id = #{sourceId}
        UNION
        SELECT target_asset_id AS asset_id
        FROM yak_metadata_relation
        WHERE source_type = #{sourceType} AND source_id = #{sourceId}
    </select>
</mapper>
